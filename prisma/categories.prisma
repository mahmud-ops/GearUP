model Categories {
  id          String      @id @default(uuid())
  name        String      @unique
  slug        String      @unique
  description String?
  gearItems   GearItems[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt()
}
